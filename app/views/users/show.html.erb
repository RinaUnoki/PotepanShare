 <h3>ユーザー詳細</h3>
 <%= attachment_image_tag @user, :image_name_change, fallback: "no-image.jpg", class: "profile_image is-rounded" %>
    
    <table>
      <tr>
        <th>ユーザー名</th>
        <td><%= @user.name %></td>
      </tr>
      <tr>
        <th>メールアドレス</th>
        <td><%= @user.email %></td>
      </tr>
      <tr>
        <th>自己紹介</th>
        <td><%= @user.introduction %></td>
      </tr>
    </table>
 
    <div>
      <ul>
        <li><%= link_to "編集", [:edit, @user] %></li>
        <li><%= link_to "トップに戻る", :users %></li>
      </ul>
    </div>